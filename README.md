# KidQuest v5.3 (Intro-Fix, Konfetti, Profil-UX)

**Neu / Fixes:**
- **Intro zuverlässig schließen** (Delegation + Fallback, `.hidden` forciert, Bindings nach DOM-Load).
- **Konfetti-Animation** bei Achievement-Unlock (rein CSS/DOM, offline).
- **Profil-Handling**: Header „Profil +“ + Admin-Profile (Erstellen/Umbenennen/Löschen) – alles stabil.
- **Admin-Freigaben** mit Daten-Attributen (keine Titel-Kollisionen).
- **Service-Worker Cache-bump** (`kidquest-v5.3`).

Alles andere bleibt: Freigabe-Flow, Limits (Tag/Woche), Badges, Erfolge, virtuelles Haustier, Dark Mode, Mehr-Profile.

## Update
1. ZIP entpacken und in GitHub **alle Dateien ersetzen**.
2. **Hard Reload**: Strg+Shift+R (Win) / Cmd+Shift+R (macOS).
3. Falls nötig: Website-Daten/Service-Worker für die Pages-URL löschen und neu laden.
